<?php

class MRoomStatusController extends GxController {
    public $baseName = "Room Status";
    public $actionName = "List Data";
    


    public function actionView($id) {
        $this->render('view', array(
            'model' => $this->loadModel($id, 'MRoomStatus'),
        ));
    }

    public function actionCreate() {
        $this->actionName = "New Data";
        $model = new MRoomStatus;


        if (isset($_POST['MRoomStatus'])) {
            $model->setAttributes($_POST['MRoomStatus']);

            if ($model->save()) {
                Yii::app()->user->setFlash('success', "Data saved!");
                $this->redirect(array('index'));
            }
        }

        $this->render('create', array( 'model' => $model));
    }

    public function actionUpdate($id) {
        $this->actionName = "Update Data";
        $model = $this->loadModel($id, 'MRoomStatus');


        if (isset($_POST['MRoomStatus'])) {
            $model->setAttributes($_POST['MRoomStatus']);

            if ($model->save()) {
                Yii::app()->user->setFlash('success', "Data saved!");
                $this->redirect(array('index'));
            }
        }

        $this->render('update', array(
            'model' => $model,
        ));
    }

    public function actionDelete($id) {
        MRoomStatus::model()->deleteByPk($id);
        Yii::app()->user->setFlash('success', "Data deleted.");
        $this->redirect(array('index'));
    }

    public function actionIndex() {
        $this->render('index');
    }
}